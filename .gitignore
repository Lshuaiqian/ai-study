# ---------------------------------------------------------------------------
# 与本项目无关的「书途 · 图书分享平台」（纯 Java / JDK 21）
#
# 这个目录里同时装着一个 Java 项目（src/ data/ web/ out/ + 三个 bat + 根 README）。
# 它不是 AI 辅助学习系统的一部分，所以一律不提交。
# 想彻底分家的话：把下面这些路径整体移到另一个目录，再把本段删掉即可。
# ---------------------------------------------------------------------------
/src/
/out/
/web/
/data/
/build.bat
/run.bat
/clean.bat
/README.md

# ---------------------------------------------------------------------------
# 密钥与本地配置
# ---------------------------------------------------------------------------
study/config.json
config.local.json
*.key
.env

# ---------------------------------------------------------------------------
# 运行状态与归档（含个人学习数据、笔记快照、旧文件备份）
# ---------------------------------------------------------------------------
study/state/
study/archive/
study/work/

# ---------------------------------------------------------------------------
# 第三方仓库副本（花笺源码、西二资料）——用 submodule 或在文档里给来源链接，
# 不要把它们塞进本仓库。
# ---------------------------------------------------------------------------
/refs/

# ---------------------------------------------------------------------------
# 工具与构建产物
# ---------------------------------------------------------------------------
__pycache__/
*.pyc
.pnpm-store/
.dsh-backups/

# 下载来的安装包（12 MB 的 rustup 安装器）与 Rust 构建产物——
# 这些是早期探针留下的，占了 tools/ 里 28 MB 中的 27 MB，不该进仓库。
/tools/rustup-init.exe
**/target/
*.o
*.obj
*.rlib
*.rmeta
*.pdb
*.bin
